#ifndef __MYNN_HELP_HPP__
#define __MYNN_HELP_HPP__
#include <vector>
#include <string>

void print_m(const std::vector<float> m, const int rows, const int cols);

bool test(const std::vector<float> &m1, const std::vector<float> &m2);

#endif